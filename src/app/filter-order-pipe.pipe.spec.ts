import { FilterOrderPipePipe } from './filter-order-pipe.pipe';

describe('FilterOrderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
